# ==============================================================================
# MINITRON SSM - CLUSTER OPTIMIZED REQUIREMENTS (CUDA 12.0 / A100 COMPATIBLE)
# ==============================================================================

# Core ML Frameworks (Strictly matched for Driver 12.0 and Mamba/Triton 3.5+)
torch==2.5.1
transformers==4.46.0
accelerate>=0.33.0
datasets>=2.20.0
tokenizers>=0.19.0
safetensors>=0.4.0

# Deep Learning Runtime Extensions
triton==3.1.0
fsspec[http]==2026.2.0
requests>=2.32.2

# Config / Utilities
pyyaml>=6.0
pydantic>=2.7
tqdm>=4.66
numpy>=1.26

# Evaluation
lm-eval>=0.4.4

# Dev / Test
pytest>=8.2

# NOTE:
# `mamba-ssm` and `causal-conv1d` are required at runtime on the GPU cluster
# for Nemotron-H forward/backward passes. They depend on a matching CUDA
# toolchain and are NOT installable on macOS / CPU-only dev boxes. Install
# them inside the A100 environment (conda env `minitron`), not on macOS / CPU-only:
#
#   pip install mamba-ssm==2.* causal-conv1d>=1.4
